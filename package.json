{
  "name": "app.js",
  "version": "0.0.0",
  "description": "",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "serve": "node dist/app.js",
    "start": "npm run build && npm run serve",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "",
  "devDependencies": {
    "typescript": "^4.0.3"
  },
  "dependencies": {
    "@ready.io/server": "0.1.1"
  }
}
